import Header from "../Header/Header";

const Calender = () => {
     return (
          <div>
               <Header title={{ month: "Jan", year: "2021" }} />
          </div>
     );
};

export default Calender;